name: Action Validation - Directory (UNIX-Like)
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Jule
        uses: julelang/setup-jule@master
        with:
          version: latest
          directory: ..