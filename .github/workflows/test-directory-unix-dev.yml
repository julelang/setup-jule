name: Action Validation - Directory (dev, UNIX-Like)
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Setup Jule
        uses: julelang/setup-jule@master
        with:
          version: dev
          directory: ..