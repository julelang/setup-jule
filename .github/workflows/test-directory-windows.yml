name: Action Validation - Directory (latest, Windows)
on: [push, pull_request]
jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Setup Jule
        uses: julelang/setup-jule@master
        with:
          version: latest
          directory: C:\